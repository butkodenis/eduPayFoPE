import { Typography } from '@mui/material';

const Courses = () => {
  return (
    <div>
      <h1>Courses</h1>
      <Typography variant="body1">
        тут будет таблица с курсами и их данными добавить курсы . организовать
        через форму.
      </Typography>
    </div>
  );
};

export default Courses;
